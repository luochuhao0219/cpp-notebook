# 总结



## 二分查找法

### 核心思想

当答案存在一个有序范围，而且满足某种**单调性**时，可以不一个个枚举答案，而是在答案范围上二分。

### 分类

- 最小化最大值
  - leetcode 1760 410
- 最大化最小值
  - leetcode 1552

### 思路

#### 1.构建二分思路

观察是 ` TTTTFFF`型还是`FFFFTTT`型

#### 2.构建check函数

涉及到

- 模拟型
- 贪心型
- 数学计算型

### 代码板

```cpp
#include <iostream>
using namespace std;
//check函数构建
bool check() {
    // 可能涉及到贪心，模拟
}
//二分查找过程
int getmid(vector<int> nums) {
    int l = 0;
    int r = max;
    int answer;
    while (l <= r) {
        int mid = (l + r) / 2;
        if (check())
        // 具体视情况而定
        {
            answer = mid;
            r = mid + 1;
        } else {
            l = mid - 1;
        }
    }
    return answer;
}
```

## 一维前缀和

### 核心思想

提前保存从开头到当前位置的累计信息，通过两个前缀状态的差，快速得到某个连续区间的信息。核心是“预处理 + 作差”，避免重复计算。

### 分类

- 区间型求和性前缀
  - leetcode 303
- 前缀和+哈希表
  - Leetcode 560等

### 思路

#### 对于类型1

直接构造前缀和数组即可

#### 对于类型2

遍历一遍数组，当前前缀和确定后，利用哈希表快速寻找“与它匹配的过去前缀状态”

### 代码

#### 对于类型一

```c++
class NumArray {
   public:
    vector<int> sumArray;
    NumArray(vector<int>& nums) {
        sumArray = vector<int>(nums.size() + 1, 0);//一定要注意多一个容量，因为0个数相加是0，所以要多一个
        for (int i = 1; i < sumArray.size(); i++) {//注意从1开始遍历刚刚好
            sumArray[i] = sumArray[i - 1] + nums[i - 1];
        }
    }
//利用前缀和数组求区间和
    int sumRange(int left, int right) {
        return sumArray[right + 1] - sumArray[left];
    }
};

```

#### 对于类型二

```cpp
#include <iostream>
#include <unordered_map>
#include <vector>
using namespace std;
int getmaxlength(vector<int>& arr, int k) {
    unordered_map<int, int> mp;  //{长度k,第一次出现的位置}
    mp.clear();
    mp.insert({0, -1});
    int sum = 0;
    int maxlength = 0;
    for (int i = 0; i < arr.size(); i++) {
        sum += arr[i];
        int aim = sum - k;
        if (mp.count(aim)) {
            if (i - mp[aim] >= maxlength) {
                maxlength = i - mp[aim];
            }
        }
        if(!mp.count(sum))
        {
            mp[sum]=i;
        }
    }
    return maxlength;
}
```

#### 适用情况

当出现子数组的时候可以使用

##  二维前缀和

### 核心思想

将一个二维数组`A[i][j]`处的值转变为以`A[0][0]`到`A[i][j]`的和的值。

### 分类

- 模版题
  - Leetcode 304
- 应用
  - Leetcode 1139

### 思路

- 1.构建前缀和数组，对于每一个`A[i][j]`进行如下操作
  - `A[i][j]=A[i][j-1]+A[i-1][j]-A[i-1][j-1]+A[i][j]//画图用容斥原理理解`
- 2.若要求row1,col1到row2,col2的值，对新和数组进行如下操作
  - `A[row2][col2]-A[row2][col1-1]-A[row1][col2-1]+A[row1-1][col1-1]`

### 代码

```cpp
#include <iostream>
#include <vector>
using namespace std;
int getnumber(vector<vector<int>>& grid, int i, int j) {
    if (i < 0 || j < 0) {
        return 0;
    }
    return grid[i][j];
}//该函数特别重要，可处理第0行第0列防止越界
int getsum(vector<vector<int>> getsummatrix, int row1, int col1, int row2,
           int col2) {
    if (row2 < row1 || col2 < col1) {
        return 0;
    }
    int answer = getsummatrix[row2][col2] -
                 getnumber(getsummatrix, row2, col1 - 1) -
                 getnumber(getsummatrix, row1 - 1, col2) +
                 getnumber(getsummatrix, row1 - 1, col1 - 1);
    return answer;
}
void getsumgrid(vector<vector<int>>& grid) {
    int m = grid.size();     // 行
    int n = grid[0].size();  // 列
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < n; j++) {
            grid[i][j] = getnumber(grid, i - 1, j) + getnumber(grid, i, j - 1) -
                         getnumber(grid, i - 1, j - 1) + grid[i][j];
        }
    }
}
```

## 一维差分

### 核心思想

当对一个数组A的`[r,l]`上进行一个操作（如+a），仅仅只需要对`A[r]+a`,然后对`A[l+1]-a`即可。(对一个数组重复操作亦然)

### 分类

- 常规题型：leetcode 1109

### 思路

- 先构造差分数组` A[i]-=A[i-1]`

- 当对一个数组A的`[r,l]`上进行一个操作（如+a），仅仅只需要对`A[r]+a`,然后对`A[l+1]-a`。
- 对数组进行累加
  - ` A[i]+=A[i-1]`

### 代码

``` cpp
#include <iostream>
#include <vector>
using namespace std;
vector<int> corpFlightBookings(vector<vector<int>>& bookings, int n) {
    vector<int> answer(n + 2, 0);//常规状态下需要多开辟一个空间（以防止r+1越界），这里如果从1开始就得开辟两个空间。
    for (auto x : bookings) {
        int l = x[0];
        int r = x[1];
        answer[l] += x[2];
        answer[r + 1] -= x[2];
    }
    for (int i = 1; i < answer.size(); i++) {
        answer[i] += answer[i - 1];
    }
    answer.erase(answer.begin());
    answer.erase(answer.end());
    return answer;
}
```

## 等差数列差分

### 核心思想

对一个一维数组A从`[i,j]`上对位想加一个等差数列。

### 分类

- 模版题
  - luogu P4231

### 思路

对于一个数组的`[l,r]`上加一个首项为s,末项为e,公差为d的一个等差数列。

- 构造差分数组`A[i]-=A[i-1]`

- `A[l]+=s`
- `A[l+1]+=d-s`
- `A[r+1]-=d+e`
- `A[r+2]+=e`
- 进行两次`A[i]+=A[i-1]`

### 代码

``` cpp
int A[100000002];
void set(int l, int r, int s, int e, int d) {
    A[l] += s;
    A[l + 1] += d - s;
    A[r + 1] -= d + e;
    A[r + 2] += e;
}
void built(int n) {
    for (int i = 1; i <= n; i++) {
        A[i] += A[i - 1];
    }
    for (int i = 1; i <= n; i++) {
        A[i] += A[i - 1];
    }
}
```



## 二维差分

### 核心思想

给定二维数组A，对`(x1,y1)到(x2,y2)`进行多次+k的操作，尽量减少操作次数

### 分类

- 模版题
  - nowcoder 二维差分
- 变式题
  - Leetcode 2132

### 思路

- 先构造差分数组，从右下往左上`A[i][j]=A[i][j]-A[i-1][j]-A[i][j-1]+A[i-1][j-1]`
- 对一次`(x1,y1)到（x2,y2)`的操作
  - `A[x1][y1]+=k`
  - `A[x1][y2+1]-=k`
  - `A[x2+1][y1]-=k`
  - `A[x2+1][y2+1]+=k`
- 从左上往右下进行`A[i][j]+=A[i-1][j]+A[i][j-1]-A[i-1][j-1]`

### 代码

``` cpp
#include <iostream>
using namespace std;
long long A[1002][1002];
void set(int x1, int y1, int x2, int y2, int k) {
    A[x1][y1] += k;
    A[x1][y2 + 1] -= k;
    A[x2 + 1][y1] -= k;
    A[x2 + 1][y2 + 1] += k;
}

void build(int n, int m) {
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= m; j++) {
            A[i][j] += A[i - 1][j] + A[i][j - 1] - A[i - 1][j - 1];
        }
    }
}
int main() {
    int n;
    int m;
    cin >> n >> m;
    int q;
    cin >> q;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= m; j++) {
            int data;
            cin >> data;
            A[i][j] = data;
        }
    }
    for (int i = n; i >= 1; i--) {
        for (int j = m; j >= 1; j--) {
            A[i][j] = A[i][j] - A[i - 1][j] - A[i][j - 1] + A[i - 1][j - 1];
        }
    }
    for (int i = 1; i <= q; i++) {
        int x1, y1, x2, y2, k;
        cin >> x1 >> y1 >> x2 >> y2 >> k;
        set(x1, y1, x2, y2, k);
    }
    build(n, m);
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= m; j++) {
            cout << A[i][j] << " ";
        }
        cout << endl;
    }
}
```

## 单调栈结构(以下只为最一般的情况，部分题稍微修改为更优)

### 核心思想

给定一个数组，若是要找`arr[i]`左侧和右侧第一个比他小(大)的值。通过维护一个具有单调性的栈(从小到大)(从大到小)来实现。

### 分类

- 模版题：https://www.nowcoder.com/practice/2a2c00e7a88a498693568cef63a4b7bb?utm_source=chatgpt.com
- 练习：https://leetcode.cn/problems/daily-temperatures/
- 特别难的练习(暂时放弃)：https://leetcode.cn/problems/sum-of-subarray-minimums/

### 思路（此处以从小到大的栈为例）

- 遍历阶段
  - 若是栈不为空且栈顶大于等于当前数组遍历到的值
    - 取栈顶为cur
    - 弹栈
    - 如果栈为空则左侧最近的小的值为-1，否则为当前栈顶
    - 右侧最近的小的值为i
  - 将遍历到的i压入
- 清算阶段(适用于遍历完成后栈中还有一些数)
  - 栈不空(一直循环)
  - 取栈顶为cur
  - 弹栈
  - 如果栈为空则左侧最近的小的值为-1，否则为当前栈顶
  - 右侧最近的小的值恒为-1
- 修正阶段(从右往左修正)（把一些相同的给删除）
  - 若是他右侧的最近比自己小值等于自己本身，则更新自己右侧的这个值为右侧的右侧的最近小值

### 代码

``` cpp
vector<vector<int>> getArray(vector<int>& arr) {
    stack<int> st;
    vector<vector<int>> answer(arr.size(), vector<int>(2, 0));
    // 遍历阶段
    for (int i = 0; i < arr.size(); i++) {
        while (!st.empty() && arr[st.top()] >= arr[i]) {
            int cur = st.top();
            st.pop();
            answer[cur][0] = st.empty() ? -1 : st.top();
            answer[cur][1] = i;
        }
        st.push(i);
    }
    // 清算阶段
    while (!st.empty()) {
        int cur = st.top();
    st.pop();
        answer[cur][0] = st.empty() ? -1 : st.top();
        answer[cur][1] = -1;
    }
    // 修正阶段
    // 只需要修正右侧即可
    // 从右往左修正
    for (int i = answer.size() - 1; i >= 0; i--) {
        if (answer[i][1] != -1 && arr[answer[i][1]] == arr[i]) {
            answer[i][1] = answer[answer[i][1]][1];
        }
    }
    return answer;
}
```

