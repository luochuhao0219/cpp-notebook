# 总结



## 二分查找法

### 核心思想

当答案存在一个有序范围，而且满足某种**单调性**时，可以不一个个枚举答案，而是在答案范围上二分。

### 分类

- 最小化最大值
  - leetcode 1760 410
- 最大化最小值
  - leetcode 1552

### 思路

#### 1.构建二分思路

观察是 ` TTTTFFF`型还是`FFFFTTT`型

#### 2.构建check函数

涉及到

- 模拟型
- 贪心型
- 数学计算型

### 代码板

```cpp
#include <iostream>
using namespace std;
//check函数构建
bool check() {
    // 可能涉及到贪心，模拟
}
//二分查找过程
int getmid(vector<int> nums) {
    int l = 0;
    int r = max;
    int answer;
    while (l <= r) {
        int mid = (l + r) / 2;
        if (check())
        // 具体视情况而定
        {
            answer = mid;
            r = mid + 1;
        } else {
            l = mid - 1;
        }
    }
    return answer;
}
```

## 前缀和

### 核心思想

提前保存从开头到当前位置的累计信息，通过两个前缀状态的差，快速得到某个连续区间的信息。核心是“预处理 + 作差”，避免重复计算。

### 分类

- 区间型求和性前缀
  - leetcode 303
- 前缀和+哈希表
  - Leetcode 560等

### 思路

#### 对于类型1

直接构造前缀和数组即可

#### 对于类型2

遍历一遍数组，当前前缀和确定后，利用哈希表快速寻找“与它匹配的过去前缀状态”

### 代码

#### 对于类型一

```c++
class NumArray {
   public:
    vector<int> sumArray;
    NumArray(vector<int>& nums) {
        sumArray = vector<int>(nums.size() + 1, 0);//一定要注意多一个容量，因为0个数相加是0，所以要多一个
        for (int i = 1; i < sumArray.size(); i++) {//注意从1开始遍历刚刚好
            sumArray[i] = sumArray[i - 1] + nums[i - 1];
        }
    }
//利用前缀和数组求区间和
    int sumRange(int left, int right) {
        return sumArray[right + 1] - sumArray[left];
    }
};

```

#### 对于类型二

```cpp
#include <iostream>
#include <unordered_map>
#include <vector>
using namespace std;
int getmaxlength(vector<int>& arr, int k) {
    unordered_map<int, int> mp;  //{长度k,第一次出现的位置}
    mp.clear();
    mp.insert({0, -1});
    int sum = 0;
    int maxlength = 0;
    for (int i = 0; i < arr.size(); i++) {
        sum += arr[i];
        int aim = sum - k;
        if (mp.count(aim)) {
            if (i - mp[aim] >= maxlength) {
                maxlength = i - mp[aim];
            }
        }
        if(!mp.count(sum))
        {
            mp[sum]=i;
        }
    }
    return maxlength;
}
```

#### 适用情况

当出现子数组的时候可以使用
